# 更新日志

## [2026-07-05] — v2.43-stable: SimulateClick 坐标转换修复

### 🐛 修复自动盖板触摸点偏移
`SimulateClick` 中的坐标转换 `raw_x = tx / displayInfo.width * g_touch_max_x` 存在两个缺陷：
1. 假设 `absX` 对应短边，当 `absX=长边` 时坐标轴互换
2. 未做 orientation 旋转变换，旋转后注入坐标错位

### 修复方案
新增 `Touch::Screen2Touch()` — `Touch2Screen` 的逆运算：
1. 逆 orientation 变换（屏幕坐标→物理统一坐标）
2. 归一化 + absX/absY 方向自适应
3. 还原到触摸驱动原始坐标

`SimulateClick` 改用 `Screen2Touch` 替代手动转换。

## [2026-07-05] — v2.42-stable: Touch2Screen 坐标轴方向自适应

### 🐛 修复触摸点偏移
原 `Touch2Screen` 假设触摸驱动 `absX` 对应屏幕短边、`absY` 对应长边。
当设备 `absX` 对应长边时，X/Y 坐标轴被互换，导致触摸点和显示点不一致。

### 修复方案
用归一化坐标重写 `Touch2Screen`：
1. 保存触摸驱动 `absX/absY.maximum`
2. 归一化坐标到 [0,1]
3. 判断 `absX` 对应物理长边还是短边（`screenX_max >= screenY_max`）
4. 自动交换坐标轴，确保 `xt=短边方向`、`yt=长边方向`
5. 保留原有 orientation 旋转变换逻辑

## [2026-07-05] — v2.41-stable: 非方形窗口 + 旋转重建 EGL Surface

### 🐛 彻底修复（v2.40 遗留问题）
v2.40 仅同步了 ImGui DisplaySize，但 native window 仍是方形 {max,max}，方形窗口在屏幕上的渲染位置与屏幕可见区域不匹配，导致悬浮窗显示位置和实际位置不一样。

### 修改内容
- **非方形窗口**: 创建真实屏幕尺寸 `{width, height}` 的窗口，替代方形 `{max, max}`
- **旋转重建**: 旋转/折叠时重建 native window + EGL surface（不销毁 GL context，保留纹理）
- **OpenGLGraphics::RecreateSurface**: 新增方法，销毁旧 surface → 创建新 surface → 绑定到现有 context
- **Touch::UpdateScreenSize**: 轻量更新触摸坐标系，不重开 /dev/input 设备
- **AndroidImgui::Recreate**: 统一重建入口，更新窗口、尺寸、DisplaySize

## [2026-07-05] — v2.40-stable: 触摸坐标系修复

### 🐛 Bug修复（跨设备触摸失效）
- **菜单按不了**: ImGui DisplaySize 为方形 {max,max}，触摸坐标为矩形 {min,max}，x方向存在死区
- **悬浮窗拖不动**: 标题栏命中检测因坐标系不匹配永远返回 false
- **触摸点偏移**: 手指位置与界面响应位置存在 max-min 像素偏差
- **不同设备表现不同**: 宽高比越大死区越大（19.5:9 屏仅 45% 可触摸区）

### 修复方案
在 `drawBegin()` 中每帧同步 `ImGui::GetIO().DisplaySize` 为真实屏幕尺寸
`{displayInfo.width, displayInfo.height}`，在 `ImGui::NewFrame()` 之前执行。
Touch2Screen 通过 orientation 参数已正确变换坐标，无需额外修改。

## [2026-07-05] — v2.39-stable: CPU亲和性优化 + 守墓人遁地修复

### 🧠 CPU亲和性升级 (4项改进, 借鉴ncnn+AndroidCpuAffinityTool)
- **直接 syscall**: `sched_setaffinity` → `syscall(__NR_sched_setaffinity)`, 绕过 bionic 兼容问题
- **三级频率回退**: `time_in_state` → `cpuinfo_max_freq` → `scaling_max_freq`, 离线核也能检测
- **中位频率法**: 替代硬编码 85% 阈值, `median=(max+min)/2`, 更准确适配 2/3 簇架构
- **循环重绑**: DrawThread 每 600 帧(≈10s) + DataThread 每 600 次迭代(≈30s) 重新强制执行亲和性, 防止内核调度器/EAS 重置
- **旗舰跳过**: 全核最低频率 ≥ 3.0GHz 的设备跳过绑核, 让调度器自行管理

### 🐛 Bug修复
- **守墓人遁地始终可见**: `h55_prop_tieqiao` 类名含prop导致误分类为阵营4, 现归类为阵营2+幽灵开关豁免

## [2026-07-04] — v2.38-stable: 核武级安全加固 + 交互增强 + 跨设备适配

### 🔒 安全加固 (7层纵深防御)
- **反调试 (7项)**: ptrace(fork+超时) + TracerPid + Frida maps/ports/FDs + LD_PRELOAD + SigCgt
  - ptrace自动跳过shell/su环境, 避免假死
  - SigCgt使用SIGTRAP+SIGSTOP双重判断, 避免Android误报
  - Frida端口27042-27052全量扫描(IPv4+IPv6)
  - Frida FDs检测高inode TCP socket(阈值≥3)
- **互锁守卫**: 5个flag分散在3个独立内存位置(堆/BSS), inline跨TU共享
- **心跳降级**: 2→4→5次失败渐进锁死, SECURE_ALPHA联动
- **完整性校验**: BKPT(0xD4200000) + NOP-slide(0xD503201F) 双重检测, 4区覆盖
- **看门狗**: 监控线程20-30s自检, 主循环90s超时检测, 60s启动宽限
- **内存防篡改**: mprotect检测(SELinux兼容, 失败不误判)
- **进程隐身**: 伪装kworker + OOM保护
- **服务端偏移下发**: 签名扫描魔术数从服务器动态获取, 回退硬编码
- **VPN/模拟器退出提示**: 红色警告 + 500ms延迟

### 🖱️ 交互增强
- 菜单栏点击标题折叠/展开 + 拖拽移动
- 天赋查看窗口可拖动

### 🌐 跨设备适配
- wood_touch百分比存储 + DPI缩放渲染 + 暖金配色

### 🔧 修复
- 绘制道具勾选无效(阵营=4无渲染)
- 触发距离默认3m

### 二进制
- **overlay-v2.38-stable**: MD5 `aedbd43596aeaa7622ed754e629219fa`

---

## [2026-07-04] — v2.37-stable: 交互增强 + 跨设备适配 + 道具绘制修复

### 🖱️ 交互增强
- **菜单栏折叠/展开**：点击标题「大米饭先生」可折叠为最小化横条，再次点击横条展开
  - 标题栏同时支持拖拽移动（快速点击=折叠，按住拖拽=移动）
  - 折叠/展开带平滑过渡动画
- **天赋查看窗口可拖动**：默认右上角，长按标题栏自由拖拽
- **隐藏 ImGui 隐式 Debug 浮窗**：移除渲染中意外出现的 "Debug" 调试窗口

### 🌐 跨设备适配（自动盖板）
- **触摸坐标改为百分比存储**：`wood_touch_x/y` 持久化使用屏幕百分比，不同分辨率设备自动适配
  - 默认坐标保持与原设备一致（10.3%/92.3% → 1080×2400 设备上约 221/2216）
  - 折叠屏/旋转时自动从百分比重新计算坐标
  - 旧版配置自动迁移（首次启动反算百分比）
  - 滑块拖动时同步更新百分比
- **触摸点渲染 DPI 缩放**：圆圈、十字线、扩散波纹尺寸全部按 `g_ui_density` 缩放
- **触摸点配色升级**：从绿色改为暖金色 `RGB(242,199,56)`，与整体 UI 主题协调

### 🔧 功能修复
- **修复「绘制道具」勾选无效**：普通道具（阵营=4）之前被完全忽略无渲染，现已修复
  - 移除 `MjSubsystem::draw_props` 在渲染条件中的冗余门禁
  - 阵营=4 普通道具新增独立渲染逻辑（显示名称+距离，不受摸金子开关影响）
  - 阵营=6 摸金道具保持原有完整逻辑不变

### ⚙️ 参数调整
- 自动盖板触发距离默认值：`2.0m` → `3.0m`
- 自动盖板随机抖动范围：`±50px` → `±50 × g_ui_density`

### 二进制
- **overlay-v2.37-stable**: MD5 `4867ff67d1782cde56cb0163208a562b`

### 🧹 清理
- 移除 v2.35-debug 中添加的所有 `MD_LOG` 诊断代码
- 删除手机上的 `mapdetect.log`
- 用户已自行解决地图识别问题

### 二进制
- **overlay-v2.36-stable**: MD5 `8c59c638d2d4c36c1d2291e5869e42ba`

---

## [2026-07-01] — v2.35-debug: 音乐盒识别诊断日志（已清理）

---
## [2026-07-01] — v2.34-stable: 修复"评分18却匹配地图1"的关键Bug

### 🔧 核心修复
- **Tier 1 音乐盒匹配改用指纹数据库**（不再直接用 g_musicbox_db→g_all_maps）
  - 旧逻辑：`g_musicbox_db[i].mapIndex` 直接映射到 g_all_maps 索引，映射可能不一致
  - 新逻辑：搜 `g_fingerprint_db` 找匹配指纹，通过 `ExecuteMapSwitch(fp_id)` → `g_mapidx_from_fp_id` 正确解析
  - 同名音乐盒多指纹时：优先用指纹DB中的钢琴坐标匹配，其次用凳子数量匹配
- **LOCKED 评分纠正**：当 `g_current_map_index >= 0` 时，若评分高分（≥65分）指向不同地图，立即切换
  - 删除了旧的 60帧/3秒 recheck 定时器（已被新逻辑替代）
- **清除 CMake 缓存**：修复了 SDK 路径缓存导致的 `externalNativeBuildCleanDebug` 失败

### 二进制
- **overlay-v2.34-stable**: MD5 `17caeb5bfd8e065ebafdda40db76ba51`

## [2026-06-29] — v2.13-stable: 路径导入（攻略图→世界坐标导航线）

### 🗺️ 新增
- **Tab 7: "路径导入"** — 从 extract_paths.py 生成的像素坐标 txt 导入导航线
  - 读取 txt 文件（`x,y` 格式），自动将像素坐标转为世界坐标
  - **黄色虚线预览**：加载后在小地图上显示预览，确认无误再导入
  - 纹理尺寸支持手动输入或"自动填充"（从已加载的地图纹理获取）
  - 像素→世界转换使用 `CoordTransform::UVToWorld()`，与现有校准参数无缝对接
  - 导入的路径自动追加到 `g_saved_paths`，持久化到 `map_config.json`
  - 导入后参与 `PathGraph` 寻路和导航

### 🔧 基础设施
- `g_map_texture_w/h[][]`: 存储每个地图/楼层的纹理像素尺寸
  - 在 `FlushTextures()` 中随纹理上传时记录
  - 在 `ResetMapState()` / `InvalidateMapTextures()` 时同步清除

### 📋 Tab 编号变更
- Tab 7: **路径导入** (新增)
- Tab 8: 调试信息 (原 7)
- Tab 9: 关于 (原 8)

### 🐍 extract_paths.py
- Python 脚本在 v2.12 阶段已完成，支持批量提取攻略图红蓝路径像素坐标

## [2026-06-29] — 无后台隐身模式 (进程伪装 + OOM保护)

### 🥷 新增
- **stealth.h**: 独立隐身模块（零侵入，调用即忘）
  - `stealth_rename()` — 进程名伪装为 `[kworker/u:0]`（ps/top 不可见）
  - `stealth_protect_oom()` — OOM score 设为 -1000，系统永不清杀
- **main.cpp**: 窗口名 `FuckNetEase` → `Surface`（降低字符串特征）
- 所有隐身操作在驱动初始化后、游戏线程启动前执行

### 🔍 影响范围排查
- ✅ anti_debug.h — 读取 /proc/self/status(TracerPid)、/proc/self/exe(大小)，不受改名影响
- ✅ draw_Gui.cpp — 扫描游戏进程 cmdline，读取的是目标进程而非自身
- ✅ driver_twt.h — 读取 /proc/self/fd，不受改名影响
- ✅ 千叶.h / ThreadAffinity.h — 扫描游戏进程，与自身进程名无关
- ✅ draw_Gui.cpp:7262 — readlink(/proc/self/exe) 读的是路径而非进程名

## [2026-06-29] — 路径统一到 /data/local/bin/

### 🔧 路径修正
- **DataManager.cpp**: `/sdcard/maps/` → `/data/local/bin/maps/`（与 MAPS_ROOT 统一）
- **my_imgui.cpp**: 字体路径 `/data/local/tmp/` → `/data/local/bin/`
- 部署目标统一为 `/data/local/bin/overlay`（SELinux 放行，反作弊检出率更低）

## [2026-06-29] — 启动界面交互优化 + 内核选择菜单

### 🎯 交互改进
- **等待确认**: 品牌横幅显示后，需按 Enter 键才继续（不再自动跳过）
- **内核选择菜单**: 实时探测可用驱动后显示交互式选择列表
  - `[1]` RT 内核 — 绿色标记"可用" / 红色"不可用"
  - `[2]` TWT 内核 — 同上
  - `[0]` 自动探测 — 按优先级尝试
- 选择的内核加载失败时自动尝试备选驱动
- `driver_registry.h` 新增 `probe_all_drivers()` 和 `driver_init_by_name()` 方法

### 🛠️ 代码优化
- main.cpp: 移除重复 `driver_init()` 调用，统一在交互阶段初始化
- driver_registry.h: 解耦探测与加载，支持按名称驱动加载

## [2026-06-29] — 启动画面品牌重命名

### 🎨 品牌更新
- main.cpp 启动画面从 "系统初始化中..." 改为品牌横幅显示：
  - 标题：**大米饭先生**
  - 下附 Telegram 链接：https://t.me/+67uRf9NT_04xMGM1
- 系统状态提示保留在横幅下方（初始化中 → 核心载入中 → 正常）

## [2026-06-29] — 回退动作ID扫描相关修改

### ↩️ 回退
- 移除 v2.10~v2.15 的动作ID扫描面板（调试标签页恢复原状）
- 这些修改包括：多链扫描、指针追踪、内存快照、联合搜索
- 原因：无法通过当前方式找到正确的动作ID读取偏移

## [2026-06-27] v2.5 — 回滚楼层阈值到 190

### ↩️ 回滚
- `GetFloorFromPlayerZ` 恢复硬编码 `Z > 190` 判定 2 楼
- 移除 v2.4 引入的 `floorZThreshold` 读取逻辑
- **原因**：用户要求回滚该改动

---

## [2026-06-27] v2.4 — 修复一楼误判为二楼

### 🐛 Bug 修复
- **★ `GetFloorFromPlayerZ` 使用地图专属阈值**：从硬编码 `Z > 190` 改为使用 `g_all_maps[current][0].floorZThreshold`
- **原因**：每张地图在 `map_config.json` 中都有 `floor_z_threshold` 字段（默认 250），但如果 1 楼海拔本身就超过 190，硬编码阈值会把玩家判为 2 楼
- **影响**：所有楼层判断点（玩家检测、瞬移重检、楼层切换）均受此修复

### ✅ 现在
- 每张地图使用自己的 `floor_z_threshold` 判断楼层
- 无地图时（`g_current_map_index < 0`）回退到默认阈值 250
- JSON 中的 `floor_z_threshold` 可单独调整（地图管理 → 校准参数）

---

## [2026-06-27] v2.3 — 修复首次地图识别为"地图1"的问题

### 🐛 Bug 修复
- **★ g_current_map_index 默认值修复**：从 `0` 改为 `-1`，避免启动时默认显示"地图1"
- **★ 移除 config 持久化恢复**：不再从 `overlay_config.txt` 恢复 `g_current_map_index`，每次启动强制重检测，防止跨局残留旧地图索引
- **★ LOCKED 首次检测逻辑**：当 `g_current_map_index == -1` 且评分 ≥ 60 分、无并列时，`ExecuteMapSwitch` 自动切换到正确地图
- **Root Cause**：初始化 `g_current_map_index = 0` → 始终指向 g_all_maps[0]（即"地图1 一楼"），LOCKED 状态只有重新检查（60 帧间隔 + 15 分差距）才会切换，从未进入首次完整识别流程

### ✅ 现在
- 启动后状态栏显示 `[LOCKED] - 等待识别 fp=-1`
- 扫描到音乐盒/凳子且评分 ≥ 60 分 → 自动切换到正确地图
- 跨局切换（`g_need_reinit`）也设置 `g_current_map_index = -1`，触发重新检测

---

## [2026-06-27] v2.2 — 新版 110 分制评分系统

### 🎯 评分重构
- **110 分制**：音乐盒40 + 凳子数量10 + **凳子位置50** + 钢琴10
- 及格线 **60 分**（对应 54.5%，与旧版 65/120 一致）
- 音乐盒容差 ≤5 满分（吸收坐标波动），>25 不计分
- 凳子匹配阈值 <4.0，权重 50 分决胜孪生地图
- **钢琴改为纯加分**：没扫到默认 10 分，不参与扣分
- 移除超量惩罚机制

### 🔄 LOCKED 定期重检
- 每 60 帧评分对比
- #1 候选 ≥ 当前 + 15 分 且 ≥ 60 分
- 持续 3 秒确认 → 执行切换
- 解决初始识别错误后无法纠正的问题

### 🧹 清理与优化
- 删除 14 个废弃全局变量
- 延迟写 JSON 脏标记（30 秒自动刷写）
- 脏标记刷写周期减少 SD 卡写入量

---

## [2026-06-27] v2.0 — 全面重构与修复

### 🏗️ 架构重构
- **新增 Structs.h** — 共享数据结构头文件
- **新增 DataManager.h/cpp** — 数据管理层，集中 JSON 序列化
- **新增 CoordTransform 命名空间** — 统一坐标转换管道
- 重构 draw_Gui.cpp 中全部重复坐标转换代码

### 🔧 核心 Bug 修复
- ★ 路径数据重启后消失：LoadMapConfigFromJSON 不再误清除出口/路径
- ★ 坐标映射偏移：所有 UV↔世界坐标使用统一 CoordTransform
- ★ 出口拖拽与地图拖动冲突：彻底分离事件，出口拖拽期间禁用地图拖动
- ★ 地图识别误报：新增 g_locked_stable_frames(≥30帧)防抖
- ★ text1_pos/title_size 变量作用域错误修复

### ✨ 新增功能
- **"数据管理"标签页** — 数据统计/导出/备份恢复
- **"调试信息"标签页** — 实时坐标/地图识别状态
- 小地图左上角增加地图/楼层/校准状态标识

### 🎨 UI 优化
- 路径列表"X"改为"删除"，选中路径高亮(★)
- 各页面主题风格统一，调试信息更完整

### 📦 构建
- CMake 自动编译新增 .cpp 文件
- 每次构建生成中文更新日志

---

## [2026-06-27] v1.8+ — 自动识别地图热修复

### 🏗️ 架构重构

**21. 完全重写自动识别地图模块**
- **核心变更**：识别触发机制从"物体位置变化"改为"玩家传送事件检测"
- 新增 `MapDetectPhase` 状态机（LOCKED → SWITCH_DETECTED → IDENTIFYING → LOW_CONFIDENCE）
- 新增 `DetectPlayerTeleport()` 检测 XY 大幅跳跃(>80) 或 Z 大幅跳跃(>50)
- 新增 200ms 防抖机制，防止快速切图频繁触发
- 新增低置信度超时机制（3秒无匹配保持当前地图）

### 📊 评分系统增强

- 新增独立指纹数据库 `g_fingerprint_db`（从 `musicbox_stools.json` 加载）
- 评分函数 `ScoreMapFingerprints` 独立为纯函数，120分制（音乐盒40+凳子数量20+凳子位置30+钢琴30）
- 建立指纹 ID ↔ g_all_maps 索引双向映射 `g_mapidx_from_fp_id` / `g_fp_id_from_mapidx`

### 🐛 问题修复

- **🎯 修复"音乐盒误报"**：只有 LOCKED 状态下才检测物体位置变化，切换/识别中忽略所有物体变化
- **🎯 修复楼层混淆**：楼层切换通过 Z 轴跳跃独立检测，与地图切换事件解耦
- **🎯 修复快速切图抖动**：新防抖机制等待稳定后再切换

### 💡 调试增强

- 左上角常驻状态显示：`[状态] 地图名 - 状态描述 fp=ID floor=N`
- 评分调试信息改由 `g_score_debug_buf` 显示
- 新地图提示改由 `LOW_CONFIDENCE` 状态触发（连续低分2秒后弹窗）

### 🔄 兼容性

- 旧的 `map_config.json` 路径/出口数据完全兼容
- 地图纹理文件（`mapN_floor*.png`）完全兼容
- 配置文件 `overlay_config.txt` 完全兼容
- 新增指纹数据文件 `/sdcard/maps/musicbox_stools.json`

### 文件变更统计
| 提交 | 描述 | 文件数 | 变更行数 |
|------|------|--------|----------|
| (本次) | 🏗️ 地图识别 v3.0 架构重构 | 2 | +~700/-500 |

---

### ✨ 新功能

**20. 路径列表管理（触屏友好）**
- 新增"已保存路径列表"UI，直接显示所有路径（路径 #0、路径 #1...）
- 每个路径旁有 **[✕]** 按钮，点击即删除
- 点击路径名可选中，方便在地图上高亮查看
- 无需右键/Delete键，纯触屏操作

---

## [2026-06-27] v1.8

### 🐛 Bug 修复

**19. FindMapByPlayerPos 错误覆盖评分识别结果**
- 根因：信号间歇性丢失时，Step2 和 Step4 的 `FindMapByPlayerPos` 使用玩家坐标找到地图，
  但若某个地图的边界范围过大（覆盖全图），它会错误返回该地图索引，覆盖了评分系统正确的识
  别结果，导致地图不断跳回错误地图
- 修复：
  - Step2（信号全丢失）：不再调用 `FindMapByPlayerPos`，保持当前地图等信号恢复
  - Step4（评分不足）：只有从未识别过地图时才用 `FindMapByPlayerPos`
  - 情况C：只有 `g_last_valid_map_index < 0` 时才重置地图，否则保持当前状态

### 文件变更统计
| 提交 | 描述 | 文件数 | 变更行数 |
|------|------|--------|----------|
| (待推) | 🐛 修复 FindMapByPlayerPos 覆盖评分结果 | 1 | +21/-21 |

---

## [2026-06-27] v1.7

### ♻️ 重构
**18. 移除 GL 上下文丢失检测/恢复机制**
- 删除 `CheckGLContextLoss()`、`RecoverFromGLContextLoss()` 函数
- 删除 `g_gl_context_lost`、`g_gl_check_counter` 全局变量
- 删除相关调用代码

### 文件变更统计
| 提交 | 描述 | 文件数 | 变更行数 |
|------|------|--------|----------|
| (待推) | ♻️ 移除 GL 上下文丢失检测机制 | 1 | +0/-39 |

---

## [2026-06-27] v1.6

### 🐛 Bug 修复

**17. 分数低于阈值 65 时自动识别不切换地图**
- 截图显示：地图1 得分 50 最高，但并未达到阈值 65，系统停留在上一张地图（地图2）
- 修复：新增"相对差距兜底"机制
- 当 #1 ≥ 40 分且与 #2 的差距 ≥ 20 分时，即便未达 65 也视为可信识别并切换
- 评分显示末尾会附加 "(差距兜底)" 提示

### 文件变更统计
| 提交 | 描述 | 文件数 | 变更行数 |
|------|------|--------|----------|
| (待推) | 🐛 修复阈值不足时不切换地图的兜底逻辑 | 1 | +24/-2 |

---

## [2026-06-27] v1.5

### 🐛 Bug 修复

**16. 评分正确但显示"地图2"的错误贴图问题**
- 根因：`g_all_maps` JSON 解析中，每层地图各占一个 slot，奇数 slot 仅 1 层（size=1）
- 但 `GetFloorFromPlayerZ(Z)` 可能返回 1，导致 `g_all_maps[idx][1]` 越界
- 越界后读到相邻 slot 的数据（恰好是"地图2"），显示名称和纹理都变成地图2
- 修复：新增 `SafeClampFloorIdx()` 函数，在所有访问 `g_all_maps` 前做楼层索引钳制
- 影响范围：`TryAutoDetectMap` 中 3 处、`LoadMapTexture`、`Draw_MapOverlay`、UI 显示

### 文件变更统计
| 提交 | 描述 | 文件数 | 变更行数 |
|------|------|--------|----------|
| (待推) | 🐛 修复 g_all_maps 索引越界导致错误地图显示 | 1 | +24/-8 |

---

## [2026-06-26] v1.3 → v1.4

### ✨ 新功能

**14. 凳子作为第三信号源 + 评分系统改进**
- 新增 `ChairKey` 结构体和 `g_chair_db` 凳子数据库
- 场景扫描自动检测 `rd01_in_pianochair01.gim` 凳子坐标
- 凳子匹配加入多信号评分：每个匹配凳子 +0.6（累加，最可靠信号）
- 评分公式升级：音乐盒(0~1.0) + 钢琴(0~0.8) + 凳子(0.6/个) + 坐标验证(+2.0)

**15. 场景物体一键同步按钮**
- 地图管理界面新增「[同步] 物体到当前地图」按钮
- 自动将当前场景钢琴+凳子坐标写入地图配置
- 写入后立即重新加载数据库，数据即时生效
- 同步到的数据后续会被识别系统自动使用

### 文件变更统计
| 提交 | 描述 | 文件数 | 变更行数 |
|------|------|--------|----------|
| (待推) | ✨ 凳子信号源+同步按钮+评分改进 | 1 | +164/-8 |

---

## [2026-06-26] v1.2 → v1.3

### 🐛 Bug 修复

**10. 地图选择器始终可见**
- 修复：原来地图选择器只在 `!g_map_auto_detect` 时显示
- 改为：无论自动识别是否开启，手动选择器始终显示
- 用户可随时覆盖自动识别的错误结果
- 相关文件：`draw_Gui.cpp`

**11. 2楼自动识别失效修复**
- 根因1：多信号评分匹配选择 `matched_db_entry` 时，只匹配 `mapIndex` 不匹配 `floorIndex`，
  始终选中一楼（第一个）的音乐盒条目，导致楼层始终被设为 0
- 根因2：钢琴兜底切换（piano-only fallback）始终设置 `g_current_floor_index = 0`
- 根因3：玩家坐标兜底（情况B）和音乐盒丢失（第二步）的 fallback 路径同样设置 floor=0
- 修复：所有路径统一根据玩家 Z 坐标 `FLOOR_Z_THRESHOLD=190.0f` 判断楼层
- 新增：`ITEM_Z_FILTER=175.0f` 统一物品过滤的 Z 轴阈值
- 相关文件：`draw_Gui.cpp`

### ✨ 新功能

**12. 手动地图选择器（始终可见）**
- 地图管理界面新增下拉列表，显示所有已配置地图
- 自动识别错误时，用户可手动选择正确地图
- 选择后自动加载对应地图纹理
- 相关文件：`draw_Gui.cpp`

**13. 场景物体一键同步**
- 新增「[同步] 物体到当前地图」按钮
- 自动保存当前场景的钢琴+凳子坐标到该地图配置
- 不需要手动指定"这个物体属于哪个地图"
- 相关文件：`draw_Gui.cpp`

### 文件变更统计
| 提交 | 描述 | 文件数 | 变更行数 |
|------|------|--------|----------|
| (待推) | 🐛 2楼自动识别修复 + 手动选择器 | 1 | +156/-89 |
| (待推) | ✨ 场景物体同步按钮 | 1 | +87/-12 |

---

## [2026-06-26] v1.1 → v1.2

### 🐛 Bug 修复

**9. 2楼自动识别失效修复**
- 根因 1：多信号评分匹配选择 `matched_db_entry` 时，只匹配 `mapIndex` 不匹配 `floorIndex`，
  始终选中一楼（第一个）的音乐盒条目，导致楼层始终被设为 0
- 根因 2：钢琴兜底切换（piano-only fallback）始终设置 `g_current_floor_index = 0`
- 根因 3：玩家坐标兜底（情况B）和音乐盒丢失（第二步）的 fallback 路径同样设置 floor=0
- 修复：所有路径统一根据玩家 Z 坐标 `FLOOR_Z_THRESHOLD=190.0f` 判断楼层
- 新增：`ITEM_Z_FILTER=175.0f` 统一物品过滤的 Z 轴阈值
- 文件：`draw_Gui.cpp`
- 推送：手机 + GitHub

### 文件变更统计
| 日期 | 提交 | 描述 | 行数 |
|------|------|------|------|
| 21:37 | (待推) | 🐛 2楼自动识别修复 | draw_Gui.cpp |

---

## [2026-06-26] v1.0 → v1.1

### 🐛 Bug 修复

**1. SaveConfig 配置文件覆盖保护**
- 修复：`SaveConfig()` 在 `g_all_maps` 为空时不再截断覆盖已有配置文件
- 防止启动初期地图数据未加载时丢失校准参数
- 相关文件：`draw_Gui.cpp`

**2. 音乐盒匹配改进：评分制 + 坐标交叉验证**
- 修复：原来"先到先得"匹配导致地图1被误判为地图2
- 改为评分制：收集所有容差内的候选条目，按距离+坐标验证综合评分
- 新增歧义保护：多地图匹配时拒绝切换
- 相关文件：`draw_Gui.cpp`

**3. 重连后地图状态残留**
- 修复：`g_need_reinit` 触发时未重置地图状态变量
- `g_current_map_index`、`g_last_valid_map_index` 等10+变量全部残留
- 修复：重连时清除纹理缓存 + 重置全部地图索引
- 相关文件：`draw_Gui.cpp`

**4. 纹理加载不检测地图变化**
- 修复：`Draw_MapOverlay` 原来只跟踪楼层变化，地图切换时不加载纹理
- 改为同时跟踪地图+楼层索引变化
- 相关文件：`draw_Gui.cpp`

### ✨ 新功能

**5. 配置文件滚动备份**
- 每次 SaveConfig 自动备份最近3个版本
- 路径：`/sdcard/maps/calib_backup/overlay_config.{bak,bak1,bak2}`
- 相关文件：`draw_Gui.cpp`

**6. GL 上下文丢失检测**
- 每60帧用 `glIsTexture` 验证纹理ID有效性
- 检测到丢失后自动清空缓存并重新加载
- 相关文件：`draw_Gui.cpp`

**7. 多信号地图识别（钢琴融合）**
- 新增 `g_piano_db` 钢琴位置数据库（第二信号源）
- 音乐盒+钢琴加权评分：音乐盒(1.0) + 钢琴(0.8) + 坐标验证(+2.0)
- 音乐盒丢失时钢琴兜底切换
- 新增 `g_detected_piano_pos` 自动检测
- 弹窗新增钢琴 X/Y/Z 编辑字段
- 相关文件：`draw_Gui.cpp`

**8. 路径绘图工具增强**
- 路径持久化：JSON 存储于 `map_config.json` 的 `player_paths` 字段
- 左键点击路径高亮选中（亮黄色加粗）
- 删除方式：`删除选中路径` 按钮 + Delete 键
- 点击空白区域取消选中
- 移除网格吸附功能（精确点击定位）
- 文件位置：`/sdcard/maps/map_config.json`（每地图独立存储）
- 相关文件：`draw_Gui.cpp`

---

### 文件变更统计
| 提交 | 描述 | 文件数 | 变更行数 |
|------|------|--------|----------|
| 84c544a | 音乐盒评分制+坐标验证 | 2 | +159/-42 |
| 224fbce | 重连后地图状态重置 | 1 | +52/-4 |
| 9dfefe3 | 备份+GL+多信号 | 1 | +265/-80 |
| 6fe4aea | 钢琴坐标自动保存 | 2 | +44/-3 |
| fe5d621 | 路径绘图增强 | 2 | +108/-56 |
