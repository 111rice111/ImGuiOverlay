========================================
  ImGuiOverlay - 版本发布记录
========================================
推送时间: 2026-06-27 11:32

当前版本: v1.8+ (热修复版)
二进制文件: /data/local/tmp/overlay
大小: ~1.1MB

--- 本版本包含以下代码变更 ---

最新修复:
  - 路径数据重启后消失：g_saved_paths.clear() 在 JSON 加载循环
    内部被误调用，导致仅最后一条 JSON 条目的路径被保留。
    已移到循环外部。

此前变更:
  - 出口标记改用 UV 空间直接渲染（消除坐标变换误差）
  - 出口标记可拖拽调整位置
  - JSON 配置备份保护（禁止启动时覆盖）
  - 途经点紫色菱形可视标记
  - 坐标调试显示（实时鼠标 UV 世界坐标）
  - emoji 替换为纯文字符号
  - 地图切换时音乐盒不被误判为"被移动"
  - 路径列表触屏友好删除按钮
  - 修复 FindMapByPlayerPos 覆盖评分识别结果 (v1.8)
  - 阈值不足时不切换地图兜底逻辑 (v1.6)
  - 修复 g_all_maps 索引越界显示错误地图 (v1.5)

========================================
