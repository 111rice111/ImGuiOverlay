# ImGuiOverlay 触摸系统诊断报告

> 排查日期: 2026-07-05
> 排查范围: 菜单按不了 / 悬浮窗不能拖动 / 触摸点与显示点不一致
> 当前版本: v2.39 (CURRENT_VERSION=239)

## 一、问题概述

用户反馈三类触摸异常，且**不同设备表现不同**：

1. **菜单按不了** — ImGui 菜单按钮点击无响应
2. **悬浮窗不能拖动** — 标题栏拖拽失效
3. **触摸点和显示点不一致** — 手指位置与界面响应位置有偏移

## 二、核心根因：三层坐标系不一致

项目存在**三套独立的坐标系**，它们彼此不匹配，导致触摸命中测试失败。

### 坐标系 A：ImGui DisplaySize（方形）= `{max, max}`

**证据链：**

`main.cpp:296-300` 创建方形 native window：
```cpp
::native_window_screen_x = (::displayInfo.height > ::displayInfo.width
    ? ::displayInfo.height : ::displayInfo.width);  // = max(w,h)
::native_window_screen_y = ::native_window_screen_x;  // ★ y = x，强制方形！
```

`AndroidImgui.cpp:19` 初始化时设置 DisplaySize：
```cpp
io.DisplaySize = {width, height};  // = {max, max} 方形
```

`AndroidImgui.cpp:31-35` 的 `NewFrame()` 默认不更新 DisplaySize：
```cpp
void AndroidImgui::NewFrame(bool resize) {  // resize 默认 false
    PrepareFrame(resize);
    My_ImGui_ImplAndroid_NewFrame(resize);  // resize=false 时跳过 DisplaySize 更新
    ImGui::NewFrame();
}
```

→ **ImGui 所有命中测试、窗口布局基于 `{max, max}` 方形空间**

### 坐标系 B：触摸注入坐标 = `{max, min}` 矩形

**证据链：**

`main.cpp:309-311` 传入 Touch::Init：
```cpp
Touch::Init({static_cast<float>(::abs_ScreenX),  // = max(w,h)
             static_cast<float>(::abs_ScreenY)},  // = min(w,h)
            true);
```

`TouchHelperA.cpp:255-259` 设置 screenSize：
```cpp
if (size.x > size.y) {
    screenSize = size;  // = {max, min}  ★ 矩形！
}
```

`TouchHelperA.cpp:378-385` 计算 touch_scale：
```cpp
if (size.x > size.y) {
    std::swap(size.x, size.y);  // size = {min, max}
}
touch_scale.x = (float)screenX / size.x;  // = screenX / min
touch_scale.y = (float)screenY / size.y;  // = screenY / max
```

`TouchHelperA.cpp:433-477` `Touch2Screen` 默认分支（orientation=0）：
```cpp
float xt = x / touch_scale.x;  // = x * min / screenX → 范围 [0, min]
float yt = y / touch_scale.y;  // = y * max / screenY → 范围 [0, max]
// orientation=0, otherTouch=false:
x = xt; y = yt;  // 返回 {x∈[0,min], y∈[0,max]}
```

`TouchHelperA.cpp:193-201` TypeA 线程直接注入到 ImGui IO：
```cpp
if (ImGui::GetCurrentContext() != nullptr) {
    ImGuiIO &io = ImGui::GetIO();
    if (device.Finger[latest].isDown) {
        auto pos = Touch2Screen(device.Finger[latest].pos);  // {min, max} 空间
        io.MousePos = ImVec2(pos.x, pos.y);  // ★ 注入到 {max,max} 的 ImGui
        io.MouseDown[0] = true;
    }
}
```

→ **io.MousePos.x 范围 `[0, min]`，但 ImGui DisplaySize.x = `max`**

### 坐标系 C：UI 布局坐标 = `displayInfo.width × height`

`draw_Gui.cpp` 全文 100+ 处使用 `displayInfo.width/height` 进行 UI 布局，例如：
- `6931`: `win_w_final = min(base*0.51*1.618, displayInfo.width * 0.9f)`
- `6935`: `anim_win_pos = ((displayInfo.width - anim_win_w) * 0.5f, ...)`
- `7044-7045`: 拖动 clamp `new_pos.x = clamp(..., 0, displayInfo.width - window_size.x)`

→ **UI 布局基于真实屏幕尺寸 `{w, h}`，既不等于方形 `{max, max}`，也不等于触摸 `{min, max}`**

## 三、三个症状的具体对应

### 症状 1：菜单按不了

**机制：**

```
ImGui 窗口位置 (方形空间 max×max)
├── 窗口 x 范围: [center_x - win_w/2, center_x + win_w/2]
│   假设 max=2400, win_w=1200 → 窗口 x ∈ [600, 1800]
├── 触摸 io.MousePos.x 范围: [0, min] = [0, 1080]
│   ★ 触摸只能命中窗口左半部分 (x ∈ [600, 1080])
└── 窗口右半部分 (x ∈ [1080, 1800]) 的按钮永远按不到
```

**命中测试失败的代码：**

`draw_Gui.cpp:7001-7004` 标题栏命中：
```cpp
bool hit_titlebar = (mouse_pos_win.x >= window_pos2.x &&  // ImGui 坐标
                     mouse_pos_win.x <= window_pos2.x + window_size.x &&
                     mouse_pos_win.y >= window_pos2.y &&
                     mouse_pos_win.y <= window_pos2.y + titlebar_height);
// mouse_pos_win 来自 io.MousePos（触摸注入，{min,max} 空间）
// window_pos2 来自 ImGui::GetWindowPos()（方形 {max,max} 空间）
// ★ 两者坐标系不匹配 → hit_titlebar 永远 false
```

### 症状 2：悬浮窗不能拖动

**机制同症状 1：**

`draw_Gui.cpp:7013` 拖动启动：
```cpp
if (ImGui::IsMouseClicked(0) && hit_titlebar) {  // hit_titlebar=false → 拖动不启动
```

`draw_Gui.cpp:7040-7051` 拖动逻辑：
```cpp
if (title_dragging) {  // 永远不会进入
    if (ImGui::IsMouseDown(0)) {
        ImVec2 new_pos = ImVec2(mouse_pos_win.x - title_drag_start_offset.x, ...);
```

### 症状 3：触摸点和显示点不一致

**偏移量计算：**

```
设屏幕: width=W, height=H, max=M=max(W,H), min=m=min(W,H)
设触摸驱动最大值: screenX, screenY (通常 = W, H)

ImGui DisplaySize = {M, M}
触摸 io.MousePos 范围 = {x∈[0,m], y∈[0,M]}  (orientation=0)

x 方向偏差 = M - m  (触摸点被压缩到 [0,m]，但显示在 [0,M])
y 方向偏差 = 0       (匹配)

例: 2400×1080 屏幕
  - M=2400, m=1080
  - 触摸 x ∈ [0, 1080]
  - 显示 x ∈ [0, 2400]
  - x 方向偏差 = 2400 - 1080 = 1320 像素
  - 触摸点显示在屏幕左侧 45% 区域，无法到达右侧 55%
```

## 四、为什么不同设备表现不同

**关键变量：屏幕宽高比 `max/min`**

| 设备类型 | 分辨率 | max/min | 可触摸区域占比 | 现象 |
|----------|--------|---------|---------------|------|
| 老设备 16:9 | 1920×1080 | 1.78 | 56% | 部分按钮按不了 |
| 主流 19.5:9 | 2400×1080 | 2.22 | 45% | 大量按钮按不了 |
| 折叠屏外屏 | 2270×832 | 2.73 | 37% | 几乎全部按不了 |
| 接近方形 | 1080×1080 | 1.00 | 100% | 完全正常 |
| 横屏平板 | 2560×1600 | 1.60 | 63% | 轻度偏差 |

**这就是"不同设备表现不同"的根因** — 宽高比越大的设备，触摸有效区域占比越小，问题越严重。

## 五、其他次要问题

### 5.1 旋转时窗口不重建

`draw_Gui.cpp:1677-1689` `drawBegin()` 检测 orientation 变化，但只移动 `g_window->Pos`：
```cpp
if (orientation != displayInfo.orientation) {
    orientation = displayInfo.orientation;
    Touch::setOrientation(displayInfo.orientation);
    if (g_window) {
        g_window->Pos.x = displayInfo.width * 0.5f - halfSize.x;  // 只移动位置
        // ★ 没有重建窗口、没有更新 io.DisplaySize
    }
}
```

旋转后 ImGui DisplaySize 仍是 `{max, max}`，但触摸坐标系已通过 orientation 变换，两者更加不一致。

### 5.2 Touch::Init 不会重新调用

`Touch::Init` 只在 `main.cpp` 启动时调用一次。旋转后 `screenSize` 和 `touch_scale` 不会更新，只通过 `setOrientation` 做坐标变换，但 `screenSize` 仍是初始化时的 `{max, min}`。

### 5.3 SimulateClick 的坐标系

`draw_Gui.cpp:5808-5809` 自动盖板点击用 `displayInfo.width/height`：
```cpp
int raw_x = (int)((float)tx / displayInfo.width * g_touch_max_x);
int raw_y = (int)((float)ty / displayInfo.height * g_touch_max_y);
```

这个用的是真实屏幕尺寸，与触摸注入坐标系不一致，可能导致自动盖板点击位置偏差。

## 六、修复方案

### 方案 A（推荐，最小改动）：每帧同步 ImGui DisplaySize

**核心修改：在 `Layout_tick_UI` 开头同步 DisplaySize**

`draw_Gui.cpp:6738` 修改为：
```cpp
void Layout_tick_UI(bool *main_thread_flag) {
    if (!ImGui::GetCurrentContext() || !g_font_ui || !g_font_ui->IsLoaded()) return;
    px = static_cast<float>(displayInfo.width) * 0.5f;
    py = static_cast<float>(displayInfo.height) * 0.5f;
    screen_config();
    drawBegin();

    // ★ 新增: 同步 ImGui DisplaySize 为真实屏幕尺寸
    ImGuiIO& io = ImGui::GetIO();
    io.DisplaySize = ImVec2((float)displayInfo.width, (float)displayInfo.height);
    io.DisplayFramebufferScale = ImVec2(1.0f, 1.0f);
    // ... 原有逻辑
```

**配套修改：旋转时重新初始化 Touch 系统**

`draw_Gui.cpp:1677-1689` `drawBegin()` 修改为：
```cpp
void drawBegin() {
    if (orientation != displayInfo.orientation) {
        orientation = displayInfo.orientation;
        Touch::setOrientation(displayInfo.orientation);
        // ★ 新增: 旋转时重新初始化 Touch，更新 screenSize/touch_scale
        // 注意: Touch::Init 会重新打开设备，开销大，建议改用轻量更新函数
        // 临时方案: 直接更新 Touch 内部状态
        // 或者: 创建 Touch::UpdateScreenSize(w, h) 轻量函数
    }
}
```

**建议在 `TouchHelperA.h/cpp` 新增轻量更新函数：**
```cpp
// TouchHelperA.h 新增
void UpdateScreenSize(const My_Vector2 &newSize);

// TouchHelperA.cpp 新增
void UpdateScreenSize(const My_Vector2 &newSize) {
    lock.lock();
    My_Vector2 size = newSize;
    if (size.x > size.y) {
        screenSize = size;
    } else {
        screenSize = {size.y, size.x};
    }
    if (size.x > size.y) std::swap(size.x, size.y);
    if (otherTouch) std::swap(size.x, size.y);
    // screenX/Y 是触摸驱动最大值，不变
    int screenX = devices[0].absX.maximum;
    int screenY = devices[0].absY.maximum;
    touch_scale.x = (float)screenX / size.x;
    touch_scale.y = (float)screenY / size.y;
    lock.unlock();
}
```

### 方案 B（更彻底）：统一所有坐标系到 io.DisplaySize

把 `draw_Gui.cpp` 中所有 `displayInfo.width/height` 替换为 `ImGui::GetIO().DisplaySize.x/y`：

```cpp
// 全局替换（约 100+ 处）
// 旧: displayInfo.width
// 新: ImGui::GetIO().DisplaySize.x
// 旧: displayInfo.height
// 新: ImGui::GetIO().DisplaySize.y
```

这样确保 UI 布局、命中测试、触摸注入三者坐标系完全一致。

### 方案 C（最稳健，工程量大）：动态窗口尺寸

旋转时销毁并重建 native window 为当前 `displayInfo` 尺寸：
```cpp
void drawBegin() {
    if (orientation != displayInfo.orientation) {
        orientation = displayInfo.orientation;
        // ★ 重建窗口
        android::ANativeWindowCreator::Destroy(::window);
        ::window = android::ANativeWindowCreator::Create(
            "Surface", displayInfo.width, displayInfo.height, false);
        // 重新初始化 OpenGL 上下文
        graphics->Shutdown();
        graphics->Init_Render(::window, displayInfo.width, displayInfo.height);
        Touch::Init({(float)displayInfo.width, (float)displayInfo.height}, true);
        Touch::setOrientation(displayInfo.orientation);
    }
}
```

**注意**：此方案开销最大（重建 EGL 上下文），但最干净。

## 七、推荐实施顺序

1. **立即实施方案 A 的核心部分**（同步 DisplaySize）— 解决 80% 问题
2. **测试验证**：在多台不同宽高比设备上测试菜单按钮、拖动、触摸精度
3. **若仍有问题，实施方案 B**（统一 UI 布局坐标）
4. **方案 C 留作长期优化**（处理旋转场景）

## 八、验证清单

- [ ] 1080×2400 竖屏设备：菜单按钮全部可点击
- [ ] 1080×2400 横屏设备：菜单按钮全部可点击
- [ ] 1440×3200 设备：触摸点和显示点偏差 < 5px
- [ ] 旋转屏幕后：菜单仍可正常操作
- [ ] 悬浮窗标题栏可拖动到屏幕任意位置
- [ ] 自动盖板点击位置准确（SimulateClick 坐标系一致）

## 九、相关代码位置索引

| 文件 | 行号 | 功能 | 问题 |
|------|------|------|------|
| main.cpp | 296-300 | 创建方形窗口 | DisplaySize = {max,max} |
| main.cpp | 309-311 | Touch::Init 传 {max,min} | 触摸坐标系为矩形 |
| AndroidImgui.cpp | 19 | io.DisplaySize 初始化 | 仅初始化一次 |
| AndroidImgui.cpp | 31-35 | NewFrame 不更新 DisplaySize | 旋转后失效 |
| TouchHelperA.cpp | 255-259 | screenSize = {max,min} | 与 ImGui 不匹配 |
| TouchHelperA.cpp | 378-385 | touch_scale 计算 | 基于 {min,max} |
| TouchHelperA.cpp | 433-477 | Touch2Screen 返回 {min,max} | 注入到 {max,max} |
| TouchHelperA.cpp | 193-201 | 直接设置 io.MousePos | 坐标系混淆 |
| draw_Gui.cpp | 6738-6743 | Layout_tick_UI 入口 | 缺 DisplaySize 同步 |
| draw_Gui.cpp | 1677-1689 | drawBegin 旋转处理 | 未重建窗口 |
| draw_Gui.cpp | 6931-6945 | 主窗口布局 | 用 displayInfo 而非 io.DisplaySize |
| draw_Gui.cpp | 7001-7004 | 标题栏命中测试 | 坐标系不匹配 |
| draw_Gui.cpp | 5808-5809 | SimulateClick 坐标转换 | 用 displayInfo |
