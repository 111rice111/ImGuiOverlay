# ImGuiOverlay 用户安装说明书

> 版本：v2.36-stable  
> 适用设备：Android 手机（需 Root 权限）  
> 官方服务器：http://43.136.183.71/

---

## 📱 一、文件说明

你需要以下文件：
- `overlay_user` — 主程序二进制文件（约 1.6MB）
- `overlay_config.txt` — 服务器配置文件

---

## 🔑 二、卡密激活

### 2.1 获取卡密
联系管理员购买，获得卡密（格式示例：`XG-XXXXXXXX-XXXXXXXX`）

### 2.2 输入卡密
1. 启动游戏后，overlay 会自动显示界面
2. 在界面中找到「卡密验证」入口
3. 输入卡密，点击「验证」
4. 验证成功提示：「授权成功」

---

## 📥 三、安装步骤（详细）

### 准备工作
- 手机已 Root（已获取 su 权限）
- 电脑已安装 ADB 驱动
- 手机已开启 USB 调试

### 步骤 1：连接手机
用 USB 数据线连接手机和电脑，在手机上允许 USB 调试。

在电脑上打开命令行（CMD），输入：
```bash
adb devices
```
看到设备序列号（如 `b5fac603`）说明连接成功。

### 步骤 2：推送二进制文件
```bash
adb push overlay_user /sdcard/overlay_tmp
adb shell "su -c 'cp /sdcard/overlay_tmp /data/local/bin/overlay_user && chmod 777 /data/local/bin/overlay_user && rm /sdcard/overlay_tmp'"
```

### 步骤 3：推送配置文件
```bash
adb push overlay_config.txt /sdcard/overlay_config.txt
adb shell "su -c 'cp /sdcard/overlay_config.txt /data/local/bin/overlay_config.txt && chmod 644 /data/local/bin/overlay_config.txt && rm /sdcard/overlay_config.txt'"
```

### 步骤 4：启动
启动游戏，overlay 会自动加载。

---

## 🖥️ 四、管理后台（管理员用）

访问：`http://43.136.183.71/?page=admin`

登录账号：`admin`  
登录密码：`admin888`（登录后请立即修改）

### 功能
- 生成卡密
- 查看在线设备
- 发布公告
- 封禁/解封用户
- 版本更新管理

---

## ❓ 五、常见问题

### Q1：提示「服务器无响应」
**解决方法**：
1. 确认手机已联网
2. 确认 `overlay_config.txt` 内容正确
3. 访问 `http://43.136.183.71/` 确认服务器在线

### Q2：卡密验证成功但功能未解锁
**原因**：设备 ID 已绑定其他卡密  
**解决**：联系管理员解绑

### Q3：提示「已达并发上限」（公用卡）
**原因**：同一卡密同时使用人数超限  
**解决**：等待其他用户下线，或联系管理员增加上限

### Q4：overlay 界面不显示
**原因**：
- 游戏未启动
- overlay 未正确安装
**解决**：
1. 确认 `overlay_user` 已在 `/data/local/bin/` 且权限为 777
2. 重启游戏

---

## 📞 六、技术支持

- Telegram 群组：https://t.me/+67uRf9NT_04xMGM1
- 管理后台：http://43.136.183.71/?page=admin

---

*ImGuiOverlay © 2026 大米饭先生*
