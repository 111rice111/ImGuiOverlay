# 2026-07-05 工作日志

## v2.39-stable: CPU亲和性优化 + 守墓人遁地修复

### 代码改动
- ThreadAffinity.h: 三级回退(time_in_state→cpuinfo_max_freq→scaling_max_freq) + 中位频率法 + syscall(sched_setaffinity) + 静态缓存 + 旗舰跳过(min≥3GHz)
- main.cpp: DrawThread 循环重绑 (600帧)
- draw_Gui.cpp: DataThread 循环重绑 (600次迭代) + 守墓人h55_prop_tieqiao分类修复

### 借鉴来源
- ncnn cpu.cpp: 三级回退 + 中位频率法 + 直接syscall
- AndroidCpuAffinityTool: 循环强制执行亲和性防止内核调度器重置

### 编译状态
- BUILD SUCCESSFUL (多次)
- 二进制路径: app/build/intermediates/cxx/RelWithDebInfo/6u3a2a4x/obj/arm64-v8a/overlay_user
- Git: bb243d7e, 已推送 GitHub
