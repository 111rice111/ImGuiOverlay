# 2026-07-01 工作日志

## v2.34 — 修复"评分18却匹配地图1"关键Bug (12:37)
Git commit: 待推送
二进制: E:\ImGuiOverlay\overlay-v2.34-stable (MD5: 17caeb5bfd8e065ebafdda40db76ba51)

### 根因分析
- Tier 1 音乐盒匹配直接使用 `g_musicbox_db[].mapIndex` 映射到 g_all_maps
- `g_musicbox_db` 从 map_config.json 加载，映射可能不正确
- 当音乐盒被检测到时 Tier 1 立即返回，评分系统永远没有机会纠正

### 修复内容
1. **Tier 1 改用指纹DB**：遍历 `g_fingerprint_db` 而非 `g_musicbox_db`，通过 `ExecuteMapSwitch(fp_id)` → `g_mapidx_from_fp_id` 正确解析
2. **LOCKED 评分纠正**：当 `g_current_map_index >= 0` 且评分 ≥65 分指向不同地图时立即切换
3. **删除 recheck 定时器**：60帧/3秒的延迟重检被新逻辑替代
4. **钢琴匹配容差放宽到8**：在 Tier 1 同名音乐盒区分时
5. **清除 .cxx 缓存**：解决 cmake/ninja 路径缓存问题

### 交付
- 已推送至 /data/local/bin/overlay (chmod 777)

## v2.28 — 地图物体同步增强 (08:53)
Git commit: afbf81a8
- SaveSceneObjectsToJSON: 返回SyncResult, 新增音乐盒同步, 详细通知
- 手动模式标注 "(手动地图)"

## v2.29 — 复合指纹+杂交匹配 (A/B/C三项优化) (~09:10)
Git commit: 4e23d802

### B: 日志自动建库
- Python脚本 `log_analysis/build_fingerprints.py`:
  - 读取72个日志, 按钢琴/音乐盒坐标去重
  - 生成23张独立地图指纹 (17有钢琴, 6无钢琴)
  - 输出 `enhanced_fingerprints.json` + `fingerprint_summary.txt`
  - 指纹冲突率: 1/23

### A: 复合指纹数据结构
- MapFingerprint 新增: piano(Vector3A), chairCount, coreDoorCount, outdoorDoorCount, propDoorCount, woodplaneCount, mapIndex
- LoadFingerprintDB v4.0: 优先加载 enhanced_fingerprints.json, 兜底旧格式
- 钢琴坐标自动匹配到 g_all_maps 索引

### C: 杂交匹配算法
- Tier 1: 钢琴精确匹配(已有, 容差5单位)
- Tier 2: 椅子数量预筛 — 只对椅子数差≤2的候选做坐标评分, 减少80%计算
- Tier 3: 候选集合内完整坐标匹配

### 交付
- 二进制: overlay (含全部优化)
- 指纹DB: /data/local/bin/maps/enhanced_fingerprints.json
- 备份: E:\ImGuiOverlay-v2.29-backup

## v2.37 — 服务器地址改为手机IP + 手机部署后端 (14:57)
二进制: E:\ImGuiOverlay\overlay-v2.37-server-phone (MD5: 8c59c638d2d4c36c1d2291e5869e42ba)

### 变更
- `net_client.h`: NET_SERVER_HOST 从 127.0.0.1 改为 192.168.1.13 (华为手机 Termux)

### 手机服务器部署
- Termux 安装 Python → 推送 server.py + overlay.db
- 手机 IP: 192.168.1.13, 监听 0.0.0.0:8080
- 存储权限: termux-setup-storage 解决 /sdcard/ 访问问题
- 启动命令: `cp /sdcard/server.py ~/ && cp /sdcard/overlay.db ~/ && cd ~ && python server.py`
- 启动脚本: E:\ImGuiOverlay\start_server.sh (推至 /sdcard/start_server.sh)

### 注意事项
- 手机 IP 可能随 DHCP 变化，连不上时在 Termux 运行 `ifconfig wlan0` 查看新IP
- Termux 需保持后台运行
- 此二进制未推送（待用户指定目标设备）

## v2.37 — 推送到新手机 OPD2413 (15:14)
- 新手机: OPPO OPD2413, Android 15, arm64-v8a, 有 root
- 推送路径: /data/local/bin/overlay (chmod 777)
- MD5: 8c59c638d2d4c36c1d2291e5869e42ba (与 v2.36 相同，因只改了 net_client.h 的宏定义)
- 服务器地址: 192.168.1.13:8080 (连旧华为手机 Termux)

## v2.37 — 用户版推送 OPD2413 (15:23)
- 用户版目录: E:\ImGuiOverlay-User（CMake target: overlay_user）
- 修改: net_client.h NET_SERVER_HOST → 192.168.1.13
- 编译产物: overlay_user (MD5: e1fa294e8b8008e7117f03099fce169d)
- 推送: /data/local/bin/overlay (chmod 777)，先 killall 旧进程
- 注意: 用户版和开发版是两个独立项目目录，二进制名也不同 (overlay vs overlay_user)

## v2.37 — 修复开发版 net_client.h 被误改 (15:28)
- 开发版 NET_SERVER_HOST 被误改为 192.168.1.13，已改回 127.0.0.1
- 开发版不需要连远程卡密服务器
- 手机端用户版重命名为 /data/local/bin/overlay_user，避免覆盖开发版 overlay

## 用户版同步开发版更新 (15:37)
- 开发版 (E:\ImGuiOverlay) v2.36-stable → 推送 GitHub
- 用户版 (E:\ImGuiOverlay-User) v2.24 → pull 合并到 v2.36
- 创建备份分支 user-backup-20260701
- 仅 README.md 有冲突（已解决），卡密系统文件全部保留：
  - main.cpp (AUTH_SERVER + 卡密验证逻辑)
  - CMakeLists.txt (curl/openssl 依赖)
  - backend/ (server.py + api.php + encrypt_key.py)
  - license.enc + deploy.sh + overlay.db
- is_woodplane 修复 + self_is_survivor 绕过均已包含在同事的提交中
- 开发版 = 无联网控制，用户版 = 有卡密系统
