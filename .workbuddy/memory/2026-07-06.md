# 2026-07-06 工作日志

## v2.43 版本回退部署 (20:26-20:35)

- 将源码切换到 v2.43 commit `b4d082a`（v2.43-stable: 触摸系统全面修复）
- 编译: `gradlew clean assembleRelease`（PowerShell, 1m42s, BUILD SUCCESSFUL）
- 编译产物: `app/build/intermediates/cxx/RelWithDebInfo/6u3a2a4x/obj/arm64-v8a/overlay_user`
- MD5: `fa5e5c4cc462db8b6964a64d241e954e`
- 推送至手机 b5fac603: `/data/local/bin/overlay_user` (chmod 777)
- 同步推送: version-latest.txt, changelog_v243.txt
- 手机端 MD5 验证通过
- 工作区已恢复到 main 分支原始状态
- 编译过程中解决: Android SDK analytics 沙箱冲突（设置 optedOut=true）

## v2.45-stable: 彻底修复跨设备触摸失效 (20:44-20:54)

### 分析阶段 (20:44-20:48)
- 深度分析 TouchHelperA.cpp/OpenGLGraphics.cpp/AndroidImgui.cpp/my_imgui_impl_android.cpp
- 识别根因: 方形窗口 → SurfaceFlinger 缩放 → 渲染像素≠屏幕像素≠触摸坐标
- 发现 6 个潜在问题，排定优先级 P0/P1/P2

### 修改阶段 (20:49-20:51)
4 文件 5 处改动:
1. main.cpp: 方形窗口 → 真实屏幕尺寸 displayInfo.width×displayInfo.height
2. OpenGLGraphics.cpp: ANativeWindow_setBuffersGeometry 显式传尺寸; 删除冗余 glViewport
3. my_imgui_impl_android.cpp: 阻止 resize 用 ANativeWindow_getWidth 覆盖 DisplaySize
4. TouchHelperA.cpp: touch_scale 按 absX_is_long 正确映射; 归一化用 (value-min)/range

### 编译推送 (20:51-20:54)
- 编译: PowerShell gradlew clean assembleRelease, 1m48s, BUILD SUCCESSFUL
- MD5: 6a13071fde961fa7bb94b9c73f8fc25e
- 推送至 b5fac603: /data/local/bin/overlay_user (chmod 777, MD5 验证通过)
- 同步推送: version-latest.txt, changelog_v245.txt
- 更新 CHANGELOG.md 记录 v2.45 变更

## 项目大整理 (23:45-23:55)

### 根目录清理
- 删除重复文件: 更新日志_v2.43/v2.45 (已存在于archive), map_config_phone2.json (与map_config_phone.json完全相同)
- 移动 CHANGELOG.txt → archive/更新日志归档/构建交付记录.txt

### 文件重命名 (中文清晰化)
- ai读.md → AI发版指南.md
- musicbox_stools_clean.json → 音乐盒凳子配置.json
- map_config_phone.json → 地图配置-手机版.json
- 重命名无代码引用, 安全操作

### archive/ 分类重组
- 历史版本二进制/: 21个ELF + 4个MD5文件
- 历史源码备份/: 2个 draw_Gui.cpp 备份
- 更新日志归档/: 24个 changelog 文件 + 完整更新日志汇总.txt (新合并)

### backend/ 清理
- 删除3个完全相同的b64文件 (server_v3_fixed.b64, server_v3_py38.b64)
- 删除 b64_raw.txt (与 b64_lines.txt 相同)

### log_analysis/ 整理
- 重命名6个 debug_* (N).log 为 debug_*_副本N.log

### 新增文件
- 项目结构说明.txt: 完整项目目录索引, 含版本历史速览
- archive/更新日志归档/完整更新日志汇总.txt: 合并所有24个更新日志

## v2.45 版本号硬编码 + 推送流程优化 (00:03-00:15)

### CMakeLists.txt 改动
- 顶部新增 OVERLAY_VERSION / OVERLAY_VERSION_CODE / OVERLAY_VERSION_STR 三个编译宏
- 通过 target_compile_definitions 传入 overlay 和 overlay_user 两个 target
- 发版前必须更新这 3 行

### main.cpp 改动
- 启动画面: "大米饭先生" → "大米饭先生 - v2.45-stable" (使用 OVERLAY_VERSION 宏)
- 强制更新提示: 显示当前版本号 OVERLAY_VERSION_STR

### AI发版指南.md 完全重写
- 新增「第零步」: 编译前更新 CMakeLists.txt 版本号
- 推送源改为 archive/历史版本二进制/overlay-v{版本号}-stable-user (版本命名的存档文件)
- 手机端文件名保持 overlay_user (兼容性)
- 新增「版本号同步检查清单」(CMakeLists + version-latest.txt + 二进制文件名)
- 新增常见错误: 忘记更新 CMakeLists.txt 版本号

### Git 推送策略变更 (00:10)
- 从"不自动 git push"改为"发版后必须 push + tag"
- 原因: 用户需要通过 tag 回退源码版本（如 git checkout v2.43-stable）
- 更新: AI发版指南.md 第六步, ~/.workbuddy/MEMORY.md, MEMORY.md

### AI发版指南完全重写 (00:13-00:17)
修复 5 个优化点:
1. **步骤拆分**: "归档"拆为 3a(存二进制) + 3b(写更新日志) + 3c(更新根目录文件)
2. **双版本推送**: 加入 overlay 开发版 + overlay_user 用户版，每次必须推两个
3. **推送前自检**: strings 验证版本号 + file 确认 ARM64 + AUTH_SERVER 检查
4. **版本回退流程**: 新增完整回退章节（git checkout tag → 推二进制 / 重编译）
5. **deploy.sh 重写**: 改为走 /sdcard/ 中转 + 双版本 + 版本命名源文件 + 不含编译和 git push
同时新增: 发版后更新 CMakeLists.txt 为下一版 dev 版本号 + 发版速查清单(9项)

### 双版本架构说明 (00:16)
- AI发版指南顶部新增「双版本架构说明」章节
- 明确: 一次 `gradlew assembleRelease` 同时产出 overlay + overlay_user
- CMakeLists.txt 中两个 target 的唯一差异是 `AUTH_SERVER` 宏
- AI 不需要分开编译、不需要切换项目

### AI发版指南修复执行 Bug (00:24)
- 合并步骤 3a+3b 为一个 Bash 调用: 变量跨命令共享
- 步骤 3 写入 `.dev_path_tmp` / `.user_path_tmp`，步骤 4 读取: 解决跨调用变量丢失
- 删除 `strings | grep AUTH_SERVER` 检查: AUTH_SERVER 是编译宏不是字符串，永远搜不到
- find 改用 `grep arm64` 过滤路径，替代 `-executable`（避免 Git Bash 兼容问题）
